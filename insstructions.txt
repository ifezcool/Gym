Fix 1 — Replace the query_ps_q4 string
Find the variable query_ps_q4 in the SQL queries section near the top of the file. It currently selects from demo_tbl_enrollee_wellness_result_data joined to demo_tbl_annual_wellness_enrollee_data with a WHERE clause filtering for records where date_submitted falls outside the policy period. Delete that entire query and replace it with a simple select of all columns from demo_tbl_enrollee_wellness_result_data where test_result_link is not null and not an empty string. No join needed.

Fix 2 — Tighten the SubmissionStatus assignment in update_provider_content
Inside the update_provider_content function, find the line that assigns pdf['SubmissionStatus'] using a lambda that checks if each MemberNo is in result_df['memberno'].values. Replace that single line with two lines: first, build a Python set of submitted member numbers by casting result_df['memberno'] to string and converting to a set (handle the case where result_df is empty by defaulting to an empty set). Then assign SubmissionStatus using that set for the lookup instead of the raw numpy array. This is both more correct and more performant.