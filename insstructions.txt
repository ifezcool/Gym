Change 1 — Add date_submitted to the provider view table
In the update_provider_content function, find the line that selects columns from filled_df into pdf. It currently selects MemberNo, MemberName, IssuedPACode, and PA_Tests. Add date_submitted to that selection. Then in the DataTable columns list below it, add a corresponding column entry for date_submitted with a display name of "Date Submitted". Do this for both the "view" option table and ensure the "not submitted" dropdown for the "submit" option only uses the non-date columns as before since it only needs member.

Change 2 — Replace radio buttons with purple buttons in provider nav and contact center nav
In ps_provider_layout, find the dbc.RadioItems with id provider-nav-option that has options for "View Wellness Enrollees and Benefits" and "Submit Wellness Results". Replace the entire RadioItems with two dbc.Button components side by side in a html.Div. Give them ids provider-nav-view-btn and provider-nav-submit-btn. Style both with the purple gradient matching the portal style (background: linear-gradient(135deg, #59058d, #800cbf)), white text, no border, rounded corners, and a small margin between them. Then update the update_provider_content callback — change its Input from provider-nav-option value to listening to both button n_clicks using callback_context to determine which was last clicked, defaulting to "view" if neither has been clicked yet.
In ps_contact_layout, the navigation is implicit (there is only one search action) so no change is needed there — the radio buttons referred to are only in the provider layout.

Change 3 — Move edit success/error messages to a more visible location
In the view_providers callback, for both the providers view and the plans view, the edit message div (services-edit-message and plans-edit-message) is currently rendered at the very bottom of the returned layout, after the DataTable and modals. Move the html.Div(id="services-edit-message") and html.Div(id="plans-edit-message") to appear immediately after the action buttons row (the row containing Add, Edit Selected, Delete Selected buttons) and before the DataTable. This way when a save succeeds, the message appears in a prominent position between the buttons and the table rather than being buried below everything.
Do the same for services-add-message and plans-add-message — move those to the same position just above the DataTable as well, so all feedback messages are consolidated in one visible zone between the action buttons and the data.

Change 4 — Add delete confirmation for Medical Services and Client Services
For the providers view in view_providers, find where services-delete-btn, services-delete-message, and the DataTable are rendered. Add a dbc.Modal with a new id like services-delete-confirm-modal that contains a header saying "Confirm Delete", a body with a message like "Are you sure you want to delete the selected provider(s)? This action cannot be undone.", and a footer with two buttons — a red "Yes, Delete" button with id services-delete-confirm-yes and a secondary "Cancel" button with id services-delete-confirm-no. Change the existing delete_providers callback so that instead of being triggered by services-delete-btn directly deleting records, it is now split into two callbacks: the first one is triggered by services-delete-btn and simply opens the confirmation modal by setting its is_open to True; the second new callback is triggered by services-delete-confirm-yes and performs the actual deletion logic (moving all the existing delete SQL code there), and services-delete-confirm-no closes the modal without deleting. Also add a callback to close the modal when Cancel is clicked.
Do exactly the same pattern for the plans view — add plans-delete-confirm-modal with the same structure, split the delete_plans callback identically so the delete button only opens the modal, and the actual deletion only happens when the confirm yes button is clicked.